language: generic
sudo: required
services:
  - docker
before_install:
  - docker build -t heshamhayat/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true heshamhayat/docker-react npm run test